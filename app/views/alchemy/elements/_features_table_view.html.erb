<% cache(element) do %>
  <%= element_view_for(element, class: 'info_table') do |el| %>
    <div class="content features">
      <h3><%= el.render :headline %></h3>
      <%= el.render :features %>
    </div>
  <% end %>
<% end %>